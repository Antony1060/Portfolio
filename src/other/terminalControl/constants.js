import color from "@/other/terminalControl/color";
import util from "@/other/terminalControl/util";

export default {
    PROMPT: util.formatTerminalText(`${color.prompt}[guest@antony.red ~]${color.reset} `),
}